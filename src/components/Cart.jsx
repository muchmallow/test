import React from "react";
import Navigation from "./Navigation";

const Cart = (props) => {
    return (
        <div>
            <Navigation/>
            CART
        </div>
    );
};

export default Cart;